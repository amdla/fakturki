8212527420


all fields with selectable values: faktura create, faktura update, pozycja create, pozycja update
should be sorted by name/number rather than ID

change stawka vat to enum
after filling form check if netto + vat = brutto
remove vat value and leave just stawka?